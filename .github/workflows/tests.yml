name: Tests

on:
  pull_request:
  workflow_dispatch:
  workflow_call:

concurrency:
  group: "${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}"
  cancel-in-progress: true


permissions:
  contents: read


jobs:

  gradle-check:
    name: "./gradlew ${{ matrix.target }} @ ${{ matrix.os }}"
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-11, windows-latest ]
      fail-fast: false
    uses: ./.github/workflows/gradle_task.yml
    with:
      runs-on: ${{ matrix.os }}
      gradle-task: >-
        check --info --stacktrace


  test-typescript-compile:
    name: "./gradlew ${{ matrix.target }} @ ${{ matrix.os }}"
    uses: ./.github/workflows/gradle_task.yml
    with:
      runs-on: ubuntu-latest
      gradle-task: >-
        :docs:code:test -Pkxstsgen_enableTsCompileTests=true  --info --stacktrace
